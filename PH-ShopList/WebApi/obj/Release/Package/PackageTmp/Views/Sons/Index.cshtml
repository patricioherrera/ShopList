@model IEnumerable<WebApi.Models.Son>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FatherId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FatherId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SonId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SonId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SonId })
        </td>
    </tr>
}

</table>
